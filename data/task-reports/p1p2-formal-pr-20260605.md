Objective

- Deliver the D+1 formal PR for terminal P1+P2 based on the accepted PoC commits `bcb9b74` and `b5a6ff7`.

Lucas Intent

- Use this task as a controlled Codex-management exercise: branch implementation must follow explicit specification only, stay visible through API-only coordination, and avoid self-directed scope drift.
- Improve the terminal output/input path from PoC quality to product-quality without regressing the protected newline/submit contract or terminal replay behavior.

Current Symptom / Evidence

- Accepted PoC exists on `main`: `apps/api/src/main.rs` has `normalize_terminal_output_newlines()` plus 3 unit tests, and `apps/web/src/main.tsx` has `scrollback=500`, `convertEol=false`, `cursorBlink=false`.
- Current API constants still keep volatile terminal retention at `4 * 1024`, and PTY reader still uses `String::from_utf8_lossy` on raw chunks.
- PTY output processing is still single-chunk broadcast in `spawn_pty_reader`, so escape-sequence fragmentation and UTF-8 boundary handling are not yet formalized.
- HQ thread `msg-1780586607856-89943438` defines the 6 formal requirements and requires 5-minute visible progress.

Why This Matters

- The PoC is accepted for immediate use, but the current implementation remains fragile around chunk boundaries, buffer retention, and transport consistency.
- This path is a protected contract because it controls agent input/output visibility and can break command delivery if changed carelessly.

Known Wrong Interpretations

- Do not treat the accepted PoC as complete product quality.
- Do not widen scope into unrelated actor/header/startupPrompt/spawn cleanup unless Lucas/CTO explicitly assigns it.
- Do not reintroduce prompt text plus Enter concatenation, raw PTY newline injection, bracketed paste submit, or CSI Enter submit.
- Do not let "batch flush" become log-only; the requirement is PTY output handling that preserves escape and UTF-8 boundaries before WS/UI replay.

Forbidden Actions

- Do not edit unrelated files outside the terminal PR scope.
- Do not restart or repoint `9001` as part of source work.
- Do not change HQ bridge behavior, meeting routes, or branch communication tooling in this task.
- Do not revert unrelated dirty worktree changes.

Source Root / Files

- Source root: `G:\Lucas-Initiative\LucasCore`
- Primary files:
  - `apps/api/src/main.rs`
  - `apps/web/src/main.tsx`
  - `docs/terminal-architecture-20260605.md`
- Evidence files:
  - `data/task-reports/p1p2-formal-pr-20260605.md`
  - `data/work-ledger.json`

Protected Contracts

- Terminal newline/submit injection.
- Terminal render/replay and retained buffer policy.
- Policy ACK and task-packet-before-edit rule.
- QA and commit gates.

Implementation Direction

- Keep the web xterm behavior aligned with the server-side normalization path.
- Replace lossy PTY chunk handling with a bounded incremental UTF-8 decode path that carries incomplete trailing bytes.
- Raise terminal volatile retention from 4 KB to 256 KB through the current runtime-config and ring-buffer path without silently desynchronizing related constants/tests.
- Introduce a small PTY output batcher in the API path so WS output/snapshot updates preserve escape-sequence chunks more safely.
- Keep `normalize_prompt_body()` as the single WS/REST prompt normalization path and prove both paths still use it.
- Expand tests to the 5 named regression cases and add architecture documentation for handoff.

Understanding Check

- Objective in my own words: formalize the accepted terminal PoC into a scoped PR that hardens PTY output normalization, retention, batching, and prompt normalization consistency.
- Lucas intent in my own words: follow the explicit 6-point spec only, show visible disciplined progress, and avoid Codex-style self-expansion or vague interpretation.
- Forbidden paths in my own words: no unrelated cleanup, no submit-contract bypass, no hidden runtime changes, no direct HQ-side editing assumptions.
- Files in scope in my own words: `apps/api/src/main.rs`, `apps/web/src/main.tsx`, and one new architecture doc.
- Protected contracts in scope: terminal newline/submit and terminal replay/retention.
- Acceptance checks in my own words: targeted Rust tests for 5 regressions, cargo test pass, relevant web tests/build pass if web code changes, and documented evidence for the PR scope.
- Questions: none at current scope.

Acceptance Evidence

- Source diff limited to the scoped PR files.
- Rust regression tests include:
  - `test_bare_cr_to_crlf`
  - `test_utf8_boundary_korean`
  - `test_ring_buffer_size`
  - `test_batch_flush_escape_sequence`
  - `test_ws_rest_normalize_consistency`
- Existing prompt/newline tests still pass.
- `cargo test` passes for the affected API module.
- If web behavior changes materially, relevant web test/build evidence is captured.
- Architecture doc explains normalization policy, function boundaries, verification SOP, and regression guardrails.

Live Progress

- 2026-06-05: Read mandatory startup policy set, role boot files, work ledger, and direct CTO task spec.
- 2026-06-05: Confirmed HQ branch self-introduction speech for `branch-dev-lead` landed in thread `msg-1780195057932-f6eb57c2`.
- 2026-06-05: Created branch `feat/p1p2-formal-pr`.
- 2026-06-05: Inspected current API/web terminal baseline and identified the exact PoC hooks that need formalization.
- 2026-06-05: Added incremental UTF-8 PTY decode, escape-aware PTY normalization, 256 KB volatile retention, 8 KB read chunks, 50 ms batch flush, and shared REST/WS prompt normalization helpers in `apps/api/src/main.rs`.
- 2026-06-05: Added `docs/terminal-architecture-20260605.md` for handoff.
- 2026-06-05: Verification passed: `cargo test --manifest-path apps/api/Cargo.toml` and `cargo check --manifest-path apps/api/Cargo.toml`.
- 2026-06-05: Policy delta recorded from HQ thread `msg-1780586973836-974f22f7`: remote branch file API is a separate D+1 track and does not block this P1+P2 formal PR, but HQ-side direct file reads are no longer an acceptable operating pattern.

Open Decisions / Blockers

- No blocker for source inspection and scoped implementation.
- Runtime deployment/restart is explicitly out of scope for this edit pass unless Lucas later orders it.
